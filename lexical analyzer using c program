 9.Write a ‘C’ program to implement lexical analyzer using c program.

 Program:

#include <stdio.h>
 #include <string.h>
 #include <ctype.h>
 char *kw[]={"if","else","for","while","int","float","char","return","void","main"};
 int iskw(char *s){
 for(int i=0;i<10;i++) if(!strcmp(s,kw[i])) return 1;
 return 0;
 }
 int isop(char c){ return (c=='+'||c=='-'||c=='*'||c=='/'||c=='='||c=='<'||c=='>'); }
 int issep(char c){ return (c=='('||c==')'||c=='{'||c=='}'||c==','||c==';'); }
 int main(){
 char c[200],t[50]; int i=0,j;
 fgets(c,200,stdin);
 while(c[i]&&c[i]!='\n'){
 if(isspace(c[i])){i++;continue;}
 if(isop(c[i])){printf("Operator: %c\n",c[i++]);continue;}
 if(issep(c[i])){printf("Separator: %c\n",c[i++]);continue;}
 j=0;
 while(c[i]&&!isspace(c[i])&&!isop(c[i])&&!issep(c[i])&&c[i]!='\n')
 t[j++]=c[i++];
 t[j]=0;
 if(iskw(t)) printf("Keyword: %s\n",t);
 else{
 int d=1;
 for(int k=0;k<j;k++)
 if(!isdigit(t[k])) d=0;
 printf("%s: %s\n",d?"Number":"Identifier",t);
 }
 }
 }


Output:

(myenv) vaishnavi@VAISHNAVI:~/cd/lab$ gcc exp9.c
 (myenv) vaishnavi@VAISHNAVI:~/cd/lab$ ./a.out
 int a=a+9;
 Keyword: int
 Identifier: a
 Operator: =
 Identifier: a
 Operator: +
 Number: 9
 Separator: ;
