 Program:

#include <stdio.h>
#include <ctype.h>
#include <stdlib.h>

int main() {
    char expr[50], c;
    void *addr[50];
    int i = 0, x = 0;

    printf("Expression terminated by $: ");
    while ((c = getchar()) != '$')
        expr[i++] = c;

    printf("Given Expression: ");
    for (int j = 0; j < i; j++)
        printf("%c", expr[j]);

    printf("\n\nSymbol Table\nSymbol\tAddress\tType\n");

    for (int j = 0; j < i; j++) {
        c = expr[j];
        if (isalpha(c)) {
            addr[x] = malloc(1);
            printf("%c\t%p\tidentifier\n", c, addr[x++]);
        } else if (c == '+' || c == '-' || c == '*' || c == '=') {
            addr[x] = malloc(1);
            printf("%c\t%p\toperator\n", c, addr[x++]);
        }
    }
    return 0;
}



 Output:

 (myenv) vaishnavi@VAISHNAVI:~/cd/lab$ gedit exp1.c
 (myenv) vaishnavi@VAISHNAVI:~/cd/lab$ gcc exp1.c
 (myenv) vaishnavi@VAISHNAVI:~/cd/lab$ ./a.out
 Expression terminated by $: e=e+t$
 Given Expression: e=e+t
 Symbol Table
 Symbol Address Type
 e
 0x55e767c9cac0 identifier
 =
 e
 +
 t
 0x55e767c9cae0 operator
 0x55e767c9cb00 identifier
 0x55e767c9cb20 operator
 0x55e767c9cb40 identifier
